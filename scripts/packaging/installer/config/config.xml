<?xml version="1.0" encoding="UTF-8"?>
<!--
  config.xml
  MNE-CPP Qt Installer Framework Configuration

  This file describes the overall installer appearance and behaviour.
  Component-specific metadata lives in packages/*/meta/package.xml.

  The directory layout mirrors Qt's own installation structure:
      ~/MNE-CPP/
      ├── @MNE_CPP_VERSION@/
      │   ├── bin/
      │   ├── lib/
      │   └── include/
      ├── data/
      ├── Tools/
      └── scripts/
-->
<Installer>
    <Name>MNE-CPP</Name>
    <Version>@MNE_CPP_VERSION@</Version>
    <Title>MNE-CPP Installer</Title>
    <Publisher>MNE-CPP Developers</Publisher>
    <StartMenuDir>MNE-CPP</StartMenuDir>
    <TargetDir>@HomeDir@/MNE-CPP</TargetDir>

    <!-- Allow the user to change the target directory -->
    <AllowNonAsciiCharacters>true</AllowNonAsciiCharacters>
    <AllowSpaceInPath>true</AllowSpaceInPath>

    <!-- Maintenance tool for add / remove / update -->
    <MaintenanceToolName>MNE-CPP-MaintenanceTool</MaintenanceToolName>
    <CreateLocalRepository>false</CreateLocalRepository>

    <!-- Wizard style -->
    <WizardStyle>Modern</WizardStyle>
    <WizardDefaultWidth>900</WizardDefaultWidth>
    <WizardDefaultHeight>600</WizardDefaultHeight>

    <!-- Branding -->
    <Logo>MNE-CPP_Logo.png</Logo>
    <StyleSheet>style.qss</StyleSheet>

    <!-- URLs -->
    <ProductUrl>https://mne-cpp.github.io/</ProductUrl>

    <!-- Interactive UI enhancements (does NOT auto-click) -->
    <ControlScript>uiscript.qs</ControlScript>

    <!--
      For headless installs, run the installer binary with the
      controlscript.qs file passed via the script flag.
    -->
</Installer>
