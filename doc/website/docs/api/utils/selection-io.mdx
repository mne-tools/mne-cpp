---
id: selection-io
title: "UTILSLIB::SelectionIO"
sidebar_label: SelectionIO
sidebar_position: 44
---

# SelectionIO

**Namespace:** `UTILSLIB` &nbsp;·&nbsp; **Library:** Utilities Library &nbsp;·&nbsp; **Header:** `utils/selectionio.h`

`#include <utils/selectionio.h>`

```cpp
class UTILSLIB::SelectionIO
```

Processes selection files (mne .sel) files which contain the channels for each selection group.

Reader/writer for MNE .sel channel-selection files and Brainstorm .mon montage files.

---

## Public Methods

### SelectionIO()

Constructs a Filter object.

---

## Static Methods

### readMNESelFile(path, selectionMap)

Reads the specified MNE sel file.

**Parameters:**

- **path** : *QString*
  holds the file path of the .sel file which is to be read.

- **selectionMap** : *QMultiMap&lt; QString, QStringList &gt; &*
  holds the map to which the read selection groups are stored.

---

### readMNESelFile(path, selectionMap)

Reads the specified MNE sel file.

**Parameters:**

- **path** : *const std::string &*
  holds the file path of the .sel file which is to be read.

- **selectionMap** : *std::multimap&lt; std::string, std::vector&lt; std::string &gt; &gt; &*
  holds the map to which the read selection groups are stored.

---

### readBrainstormMonFile(path, selectionMap)

Reads the specified Brainstorm montage file.

**Parameters:**

- **path** : *QString*
  holds the file path of the .mon file which is to be read.

- **selectionMap** : *QMultiMap&lt; QString, QStringList &gt; &*
  holds the map to which the read selection groups are stored.

---

### readBrainstormMonFile(path, selectionMap)

Reads the specified Brainstorm montage file.

**Parameters:**

- **path** : *const std::string &*
  holds the file path of the .mon file which is to be read.

- **selectionMap** : *std::multimap&lt; std::string, std::vector&lt; std::string &gt; &gt; &*
  holds the map to which the read selection groups are stored.

---

### writeMNESelFile(path, selectionMap)

Writes the specified selection groups to a single MNE .sel file.

**Parameters:**

- **path** : *QString*
  holds the file path of the .sel file which is to be read.

- **selectionMap** : *const QMultiMap&lt; QString, QStringList &gt; &*
  holds the map to which the read selection groups are stored.

---

### writeMNESelFile(path, selectionMap)

Writes the specified selection groups to a single MNE .sel file.

**Parameters:**

- **path** : *const std::string &*
  holds the file path of the .sel file which is to be read.

- **selectionMap** : *const std::map&lt; std::string, std::vector&lt; std::string &gt; &gt; &*
  holds the map to which the read selection groups are stored.

---

### writeBrainstormMonFiles(path, selectionMap)

Writes the specified selection groups to different Brainstorm .mon files.

The amount of written files depend on the number of selection groups in selectionMap

**Parameters:**

- **path** : *QString*
  holds the file path of the .mon file which is to be read.

- **selectionMap** : *const QMultiMap&lt; QString, QStringList &gt; &*
  holds the map to which the read selection groups are stored.

---

### writeBrainstormMonFiles(path, selectionMap)

Writes the specified selection groups to different Brainstorm .mon files.

The amount of written files depend on the number of selection groups in selectionMap

**Parameters:**

- **path** : *const std::string &*
  holds the file path of the .mon file which is to be read.

- **selectionMap** : *const std::map&lt; std::string, std::vector&lt; std::string &gt; &gt; &*
  holds the map to which the read selection groups are stored.

---

## Authors of this file

_No authors recorded in the source file._
