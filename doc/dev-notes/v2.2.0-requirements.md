# MNE-CPP v2.2.0 — "Intracranial + Intelligence" — Requirements & Task Sheet

Release target: v2.2.0
Codename: Intracranial + Intelligence
Branch: staging (all work happens on staging — never push directly to main)

This document is the authoritative task sheet for the v2.2.0 release. Each task
is described in enough detail that an automated coding agent can implement it with
minimal ambiguity. Tasks reference existing files, classes, namespaces, and
conventions so the agent knows exactly where to add code and how it should look.

Last updated: 10 April 2026

---

## Conventions & Patterns for All New Code

Before implementing any task, follow these project-wide rules:

### File layout
- License header: BSD-3-Clause, copyright "Christoph Dinh" (or original authors
  if porting). Use the `@file`, `@author`, `@since 2.2.0`, `@date` Doxygen block
  found in every existing header (see `src/libraries/dsp/welch_psd.h` for the
  current template).
- Include guard: `#ifndef FILENAME_H` / `#define FILENAME_H`.
- Section separators: the `//====...` bar (96 `=` chars) between logical blocks.

### Naming
- **Library directory**: lowercase, ≤5 letters (e.g. `mna`, `ml`, `stats`).
- **CMake target**: `mne_<dir>` (e.g. `mne_mna`, `mne_ml`, `mne_stats`).
- **Namespace**: `<DIR>LIB` all-caps (e.g. `MNALIB`, `MLLIB`, `STATSLIB`).
- **Export macro**: `<DIR>SHARED_EXPORT` (e.g. `MNASHARED_EXPORT`).
- **Global header**: `<dir>_global.h` / `<dir>_global.cpp`.
- **Class prefix**: capitalised `<Dir>` (e.g. `MnaProject`, `MlModel`,
  `StatsCluster`). Exception: FIFF classes use `Fiff` prefix.
- **Filenames**: all lowercase with underscores matching the class
  (e.g. `MnaProject` → `mna_project.h` / `mna_project.cpp`).

### CMakeLists.txt for a new library
Follow the pattern in `src/libraries/dsp/CMakeLists.txt`:
1. `cmake_minimum_required(VERSION 3.14)` + `project(mne_<dir> LANGUAGES CXX)`.
2. Enable `CMAKE_AUTOUIC`, `CMAKE_AUTOMOC`, `CMAKE_AUTORCC`.
3. Find Qt components (`Core` minimum; add others only if needed).
4. `set(SOURCES ...)` / `set(HEADERS ...)` — list every `.cpp` / `.h`.
5. `set(FILE_TO_UPDATE <dir>_global.cpp)` + the `OBJECT_DEPENDS` trick.
6. `add_library(${PROJECT_NAME} ${SOURCES} ${HEADERS})`.
7. `target_include_directories(... PUBLIC $<BUILD_INTERFACE:...>)`.
8. `target_link_libraries(... PRIVATE Qt6::Core eigen <dependency targets>)`.
9. `target_compile_definitions(... PRIVATE MNE_<DIR>_LIBRARY ...)`.
10. `install(TARGETS ...)` and `install(FILES ${HEADERS} ...)`.
After creating the library, add `add_subdirectory(<dir>)` in
`src/libraries/CMakeLists.txt` at the correct dependency level.

### Test conventions
Unit tests live in `src/testframes/` mirroring the library path.
Each test links `mne_<dir>` + `Qt6::Test` and uses `QTest` macros.

**Coverage target: 75% line coverage across all new and modified libraries.**

All tests must be **meaningful and data-driven**:
- Use real MNE-CPP test data from `mne-cpp-test-data/` (FIFF files, surfaces,
  source spaces, forward solutions, covariance matrices) — not just trivial
  synthetic matrices.
- When real data is impractical (e.g. permutation tests), use carefully
  constructed synthetic data with known ground truth and verify against
  published reference values or cross-validated with MNE-Python/SciPy outputs.
- Every public method must have at least one test that exercises it with
  realistic inputs.
- Edge cases: empty inputs, single-channel data, rank-deficient covariance,
  zero-length epochs.
- Performance assertions where applicable (e.g. "PSD of 64ch × 60s at 1kHz
  completes in < 2s").
- Each test file should include a brief comment at the top explaining what
  test data it uses and what ground truth it validates against.

**Never skip tests — fix instead.** If a test fails or data is missing, do not
use `QSKIP()` to paper over it. Diagnose the root cause and fix the test or the
code under test. A skipped test is an invisible hole in coverage. The only
acceptable use of `QSKIP()` is when the entire test-data directory is absent
(CI matrix job that intentionally runs without data).

**Ground implementations on MNE-SVN (C) and MNE-Python wherever possible.**
Both codebases live in this workspace and serve as reference implementations:
- **MNE-SVN** (`/MNE/MNE/SVN-MNE/`): Original C library by Hämäläinen.
  Key files: `MNE-libs/mne/mne_inverse_io.c`, `mne_inverse_util.c`,
  `mne_cov_matrix.c`, `mne_whiten.c`, `mne_source_space.c`,
  `mne_forward_util.c`, `mne_stc_util.c`. Tools: `mne_inverse_operator`,
  `mne_compute_mne`, `mne_compute_raw_inverse`, `mne_make_source_space`.
- **MNE-Python** (`/mne-python/mne/`): Python ecosystem.
  Key module: `mne.minimum_norm` (`apply_inverse`, `make_inverse_operator`,
  `read_inverse_operator`). Tests: `mne/minimum_norm/tests/test_inverse.py`
  contains numerical ground-truth checks:
  - dSPM output range `7.5 < max < 15`;
  - sLORETA output range `2 < max < 7`;
  - Localization bias tests with known dipole (dSPM fixed-ori → 96–98%
    correct, sLORETA → 100%).
  Test data: `sample_audvis_trunc-meg-eeg-oct-4-fwd.fif`,
  `sample_audvis_trunc-meg-eeg-oct-4-meg-inv.fif`, `sample_audvis_trunc-ave.fif`,
  `sample_audvis_trunc-cov.fif`, `sample_audvis_trunc_raw.fif`. SNR=3, λ²=1/9.

When implementing an algorithm that exists in MNE-Python or MNE-SVN C, the test
should validate that the mne-cpp output matches the reference output within
stated tolerances (e.g. `assert_allclose(rtol=1e-5)` equivalent). If exact
matching is impossible (e.g. different floating-point paths), document the
expected deviation and set a sensible tolerance.

**Coverage measurement**: CI runs `gcov`/`lcov` with `WITH_CODE_COV=ON`.
The 75% target applies per-library; new libraries (`mna`, `ml`, `stats`)
must ship with tests that meet this bar before merge.

---

## TASK 1 — New Library: `mna` (Analysis Container Format)

### Goal
Create the `mna` library (`src/libraries/mna/`) that reads and writes `.mna`
(JSON) and `.mnx` (BSON) project container files. A container bundles references
to FIFF raw data, forward solutions, inverse operators, covariance matrices,
source estimates, BEM surfaces, and FreeSurfer reconstructions into a single
project manifest with full provenance tracking and transparent parameter storage.

### Why — The Decluttering Problem
A single source estimation analysis today requires the user to manage a staggering
number of separate files scattered across potentially different directories on
different machines:

- **MRI data**: T1 volume (`.mgz`), FreeSurfer surfaces (pial, white, inflated),
  annotations, labels, BEM surfaces, volume parcellations
- **Raw M/EEG**: one or more `.fif` files (often gigabytes each)
- **Events/annotations**: event files, annotation sidecars
- **Preprocessing outputs**: SSP projectors, ICA solutions, bad channel lists
- **Covariance matrices**: noise covariance, data covariance
- **Forward model**: source space, BEM solution, forward operator
- **Inverse operator**: inverse solution, regularisation parameters
- **Source estimates**: STC files, one per condition/epoch
- **Connectivity**: network results

On top of scattered files, each processing step involves **parameters** (e.g.
regularisation λ=1/9, noise covariance method=Ledoit-Wolf, source space=oct-6,
BEM layers=3) that are currently invisible — buried in command-line flags or
forgotten entirely. Reproducing someone else's analysis (or your own from 6 months
ago) requires detective work.

The `.mna`/`.mnx` format solves both problems:
1. **Single manifest**: one file links every artifact in the pipeline together
   with relative paths, regardless of where the actual files live on disk.
2. **Transparent parameters**: every processing step records its tool, version,
   and full parameter dictionary so the pipeline is self-documenting.
3. **Packaging (v2.2.0+)**: the `.mnx` binary format is designed to evolve into
   a true container archive (like MP4 for video) that embeds all referenced
   files directly, eliminating the need for external file links entirely.

### Files to create

| File | Purpose |
|------|---------|
| `mna_global.h` | Export/import macro `MNASHARED_EXPORT`, `MNE_MNA_LIBRARY` define |
| `mna_global.cpp` | Build-info stamp (same pattern as `dsp_global.cpp`) |
| `mna_types.h` | Enums: `MnaFileRole` (Raw, Forward, Inverse, Covariance, SourceEstimate, Bem, Surface, Annotation, Custom), `MnaContainerFormat` (Json, Bson) |
| `mna_project.h/.cpp` | `MnaProject` class — the root container object |
| `mna_subject.h/.cpp` | `MnaSubject` — subject demographics + FreeSurfer dir |
| `mna_session.h/.cpp` | `MnaSession` — one recording session |
| `mna_recording.h/.cpp` | `MnaRecording` — one continuous acquisition (links to FIFF) |
| `mna_file_ref.h/.cpp` | `MnaFileRef` — typed reference to an external file (relative path + SHA-256 hash + role enum + optional embedded data for future .mnx packaging) |
| `mna_step.h/.cpp` | `MnaStep` — one processing step in the provenance DAG (tool name, version, parameter dict, input refs, output refs, timestamp) |
| `mna_io.h/.cpp` | `MnaIO` — single-schema dual-serializer: `read()`/`write()` for `.mna` (JSON via QJsonDocument) and `.mnx` (CBOR via QCborValue); format auto-detected from file extension |
| `CMakeLists.txt` | Build config (depends on `mne_utils`, `Qt6::Core`) |

### Class design: `MnaProject`

```
class MNASHARED_EXPORT MnaProject
{
public:
    // Shared pointer convenience
    typedef QSharedPointer<MnaProject> SPtr;
    typedef QSharedPointer<const MnaProject> ConstSPtr;

    // Construction
    MnaProject();

    // Schema versioning (checked on read; mismatch → error or graceful degradation)
    static constexpr const char* CURRENT_SCHEMA_VERSION = "1.0";

    // Project metadata
    QString name;                       // Project display name
    QString description;                // Free-form description
    QString mnaVersion;                 // Schema version ("1.0") — written on save, checked on load
    QDateTime created;                  // Creation timestamp
    QDateTime modified;                 // Last-modified timestamp

    // Hierarchy
    QList<MnaSubject> subjects;         // One or more subjects
    // Each MnaSubject contains QList<MnaSession>
    // Each MnaSession contains QList<MnaRecording>
    // Each MnaRecording contains QList<MnaFileRef>

    // Provenance — every processing step with full parameter transparency
    QList<MnaStep> pipeline;            // Ordered processing steps (DAG)

    // I/O (format auto-detected from file extension: .mna → JSON, .mnx → CBOR)
    static MnaProject read(const QString& path);
    static bool write(const MnaProject& project, const QString& path);
};
```

### Class design: `MnaFileRef`

```
struct MNASHARED_EXPORT MnaFileRef
{
    MnaFileRole role;           // What this file is: Raw, Forward, Inverse, Covariance, etc.
    QString     path;           // Relative POSIX path to the file
    QString     sha256;         // SHA-256 hex digest (verified on read, warn if mismatch)
    QString     format;         // File format hint: "fiff", "mgh", "stc", "json", etc.
    qint64      sizeBytes;      // File size in bytes (for integrity checking)

    // Packaging support (v2.2.0: reserved; v2.3.0+: .mnx can embed file content)
    bool        embedded;       // false = external file reference (default)
                                // true  = file content stored in `data` (future .mnx packaging)
    QByteArray  data;           // Raw file bytes when embedded=true; empty otherwise
                                // Writer ignores when embedded=false
                                // Reader ignores data key when embedded=false
};
```

### Class design: `MnaStep`

```
struct MNASHARED_EXPORT MnaStep
{
    QString     id;             // Unique step identifier ("step-001")
    QString     tool;           // Tool name ("mne_forward_solution", "mne_inverse_operator")
    QString     toolVersion;    // Version of the tool ("2.2.0")
    QVariantMap parameters;     // ALL parameters as key-value (transparent storage)
                                // Keys include the flag prefix: "--regularisation", "--method"
                                // Values are typed: double, int, QString, bool
    QStringList inputs;         // Relative paths to input files
    QStringList outputs;        // Relative paths to output files
    QDateTime   timestamp;      // When this step was executed
};
```

### JSON schema sketch (`.mna` file)

```json
{
  "mna_version": "1.0",
  "name": "auditory-study",
  "description": "Auditory oddball paradigm, N=12",
  "created": "2026-04-10T14:30:00Z",
  "modified": "2026-04-10T16:45:00Z",
  "subjects": [
    {
      "id": "sub-01",
      "freesurfer_dir": "derivatives/freesurfer/sub-01",
      "sessions": [
        {
          "id": "ses-01",
          "recordings": [
            {
              "id": "run-01",
              "files": [
                {
                  "role": "raw",
                  "path": "sub-01/ses-01/meg/sub-01_ses-01_task-aud_run-01_meg.fif",
                  "sha256": "a1b2c3...",
                  "format": "fiff"
                },
                {
                  "role": "forward",
                  "path": "derivatives/sub-01/sub-01-fwd.fif",
                  "sha256": "d4e5f6..."
                },
                {
                  "role": "inverse",
                  "path": "derivatives/sub-01/sub-01-inv.fif",
                  "sha256": "789abc..."
                }
              ]
            }
          ]
        }
      ]
    }
  ],
  "pipeline": [
    {
      "id": "step-001",
      "tool": "mne_forward_solution",
      "tool_version": "2.2.0",
      "parameters": { "--src": "oct6", "--bem": "3-layer" },
      "inputs": ["sub-01/ses-01/meg/sub-01_ses-01_task-aud_run-01_meg.fif"],
      "outputs": ["derivatives/sub-01/sub-01-fwd.fif"],
      "timestamp": "2026-04-10T15:00:00Z"
    }
  ]
}
```

### Architecture: Single Schema, Dual Serializer

The `.mna` and `.mnx` formats share **exactly the same internal data model**
(key names, hierarchy, value types). The only difference is the serializer:

| Aspect | `.mna` (JSON) | `.mnx` (BSON) |
|--------|---------------|---------------|
| Encoding | UTF-8 text | Binary |
| Editability | Human-readable; open in any text editor or VS Code | Requires hex editor or MNE-CPP app |
| Numeric storage | Strings ("3.14") | Native 4/8-byte IEEE floats/ints |
| Binary data | Base64-encoded (33% overhead) | Native Binary type (zero overhead) |
| Primary use | Metadata, parameters, small projects, human review | Large projects, embedded data payloads, interchange |

The code uses a single `MnaProject` data model. `MnaIO::write()` checks the
file extension and toggles between `QJsonDocument` serialization (`.mna`) and
`QCborValue` serialization (`.mnx`). `MnaIO::read()` does the same in reverse.
This means every feature works identically for both formats.

### `.mnx` Magic Header

To prevent confusion with arbitrary BSON files, `.mnx` files start with a
4-byte magic identifier before the CBOR/BSON payload:

```
Bytes 0-3: "MNX1"   (ASCII magic string + format version digit)
Bytes 4-N: CBOR payload (QCborValue serialization of the MnaProject)
```

`MnaIO::read()` checks the magic before parsing. If the magic is missing or
unrecognized, it returns an error with a clear message ("Not a valid .mnx file
or unsupported version"). The version digit (`1`) allows future schema changes
without breaking old readers.

### `.mnx` as a Packaging Format (Roadmap)

In v2.2.0, `.mnx` stores the same manifest as `.mna` — file references with
relative paths pointing to external files on disk.

In a future release (v2.3.0+), `.mnx` will evolve into a true **packaging
container** (analogous to MP4 for multimedia) that can **embed** the actual
file contents directly:

```
"files": [
  {
    "role": "raw",
    "path": "sub-01_meg.fif",
    "embedded": true,
    "data": <BSON Binary, native bytes of the .fif file>,
    "sha256": "a1b2c3...",
    "size_bytes": 1073741824
  }
]
```

BSON's native Binary type stores the raw bytes at zero overhead (no Base64),
making this efficient even for large FIFF files or MRI volumes. A single `.mnx`
file would then contain the entire analysis — raw data, MRI, surfaces, forward
model, inverse, source estimates — fully self-contained and portable, like
handing someone a single USB stick instead of a folder tree.

**v2.2.0 scope**: The `MnaFileRef` struct gains an `embedded` boolean field
(default `false`) and an optional `QByteArray data` field. The writer ignores
`data` when `embedded` is false. The reader ignores the `data` key if present
but `embedded` is false. This reserves the field for v2.3.0 without adding
complexity now.

### Parameter Transparency

The `MnaStep` provenance entry stores **all** parameters for each processing
step as a flat `QVariantMap`. This includes parameters that are often implicit
or forgotten:

```json
{
  "id": "step-002",
  "tool": "mne_inverse_operator",
  "tool_version": "2.2.0",
  "parameters": {
    "--regularisation": 0.1111,
    "--method": "MNE",
    "--noise-cov-method": "ledoit-wolf",
    "--loose": 0.2,
    "--depth": 0.8,
    "--src-type": "surface",
    "--src-spacing": "oct6",
    "--bem-layers": 3,
    "--rank": 64
  },
  "inputs": ["sub-01-fwd.fif", "sub-01-noise-cov.fif"],
  "outputs": ["sub-01-inv.fif"],
  "timestamp": "2026-04-10T15:30:00Z"
}
```

This makes every analysis decision visible and reproducible: another researcher
(or the same researcher 6 months later) can open the `.mna` file in a text
editor and see exactly what parameters produced each result.

### Versioning

The `mna_version` field at the root of every `.mna`/`.mnx` file tracks the
schema version. The reader must:
1. Check `mna_version` before parsing.
2. If major version differs (e.g. "2.x" vs "1.x"), refuse to load and report
   the version mismatch.
3. If minor version is newer (e.g. "1.2" vs "1.0"), load gracefully and ignore
   unknown keys (forward compatibility).

v2.2.0 ships with schema version `"1.0"`.

### Acceptance criteria
- `MnaProject::read("test.mna")` round-trips a project losslessly.
- `MnaProject::read("test.mnx")` round-trips with BSON.
- `.mnx` magic header is validated; reading a non-MNX binary file returns a
  clear error (not a crash or silent corruption).
- SHA-256 hashes are verified on read (warn if mismatch, don't fail).
- All paths stored as relative POSIX paths (forward slashes).
- `mna_version` is written and checked on read; version mismatch handling works.
- `MnaStep` parameter dictionaries round-trip with correct types (doubles stay
  doubles, strings stay strings, ints stay ints).
- **Data-driven test**: Build a realistic project referencing actual test data
  from `mne-cpp-test-data/` (raw FIFF, forward solution, covariance, STC),
  populate 3+ pipeline steps with realistic parameters, write to `.mna`,
  read back, verify every field. Repeat for `.mnx`.
- **Edge case tests**: empty project (no subjects), project with 5 subjects ×
  3 sessions × 2 runs, file ref with non-ASCII path characters.
- **Coverage**: ≥75% line coverage for all `mna` library source files.

---

## TASK 2 — New Library: `ml` (Machine Learning)

### Goal
Create the `ml` library (`src/libraries/ml/`) providing ONNX Runtime integration
for both inference and training, plus lightweight built-in models (linear
classifiers, basic neural nets) that can be trained on M/EEG features and exported
to ONNX format.

### Why
ML-based artifact rejection, source imaging, and brain-computer interfaces are
becoming standard in neuroscience. Today mne-cpp has zero ML capability. This
library bridges that gap by letting users (a) run pre-trained ONNX models for
inference and (b) train simple models directly in C++ for real-time BCI or
offline analysis, then export them as portable `.onnx` files.

### Files to create

| File | Purpose |
|------|---------|
| `ml_global.h` | Export macro `MLSHARED_EXPORT` |
| `ml_global.cpp` | Build-info stamp |
| `ml_types.h` | Enums: `MlBackend` (OnnxRuntime, BuiltIn), `MlDataType` (Float32, Float64, Int64), `MlTaskType` (Classification, Regression, FeatureExtraction) |
| `ml_tensor.h/.cpp` | `MlTensor` — lightweight N-dimensional tensor wrapping `Eigen::MatrixXf` with shape metadata; conversion to/from ONNX tensor format |
| `ml_model.h/.cpp` | `MlModel` — abstract base class with `predict(MlTensor)→MlTensor`, `train(MlTensor X, MlTensor y)`, `save(path)`, `load(path)` |
| `ml_onnx_model.h/.cpp` | `MlOnnxModel : MlModel` — wraps ONNX Runtime `Ort::Session` for inference; `train()` is no-op (training done externally) |
| `ml_linear_model.h/.cpp` | `MlLinearModel : MlModel` — ridge/logistic regression trained via normal equations or gradient descent; exports to ONNX via `save()` |
| `ml_pipeline.h/.cpp` | `MlPipeline` — sequential chain: feature scaler → optional PCA → model; `fit()` and `predict()` methods |
| `ml_scaler.h/.cpp` | `MlScaler` — StandardScaler (z-score) and MinMaxScaler; serializable to ONNX pre-processing nodes |
| `CMakeLists.txt` | Build config; ONNX Runtime is optional (`if(USE_ONNXRUNTIME)` guard) — built-in models work without it |

### Dependencies
- **Required**: `mne_utils`, `mne_math`, `Qt6::Core`, `eigen`.
- **Optional**: ONNX Runtime C API (`onnxruntime.h`). Guarded by
  `USE_ONNXRUNTIME` CMake option. When off, `MlOnnxModel` is a stub that
  throws `std::runtime_error("ONNX Runtime not available")`.

### Class design: `MlModel` (abstract base)

```
class MLSHARED_EXPORT MlModel
{
public:
    typedef QSharedPointer<MlModel> SPtr;

    virtual ~MlModel() = default;

    // Inference: input tensor → output tensor
    virtual MlTensor predict(const MlTensor& input) const = 0;

    // Training: features X (n_samples × n_features), labels y (n_samples × n_outputs)
    virtual void train(const MlTensor& X, const MlTensor& y) = 0;

    // Persistence
    virtual bool save(const QString& path) const = 0;    // .onnx or .json
    virtual bool load(const QString& path) = 0;

    // Metadata
    virtual QString modelType() const = 0;       // "onnx", "linear", "logistic"
    virtual MlTaskType taskType() const = 0;
};
```

### `MlOnnxModel` specifics
- `load()` creates an `Ort::Session` from the `.onnx` file.
- `predict()` maps `MlTensor` to `Ort::Value`, runs session, maps back.
- `train()` is a no-op — prints warning "Training not supported for pre-trained ONNX models".
- `save()` is a no-op for externally-trained models (the file already exists).

### `MlLinearModel` specifics
- Supports `MlTaskType::Classification` (logistic regression with sigmoid) and
  `MlTaskType::Regression` (ridge regression).
- `train()` uses Eigen's `colPivHouseholderQr()` for ridge; iterative
  reweighted least squares (IRLS) for logistic.
- `save()` exports as ONNX using the `onnx.proto3` linear regressor/classifier
  operator, or as a lightweight JSON fallback (weights matrix + bias vector).
- `load()` reads either format.

### `MlTensor` design
```
class MLSHARED_EXPORT MlTensor
{
public:
    MlTensor();
    MlTensor(const Eigen::MatrixXf& mat);
    MlTensor(const Eigen::MatrixXd& mat);        // converts to float32

    // Shape access
    int rows() const;
    int cols() const;
    QVector<int> shape() const;                   // for N-d (currently 2-d)

    // Data access
    const Eigen::MatrixXf& toMatrixXf() const;
    Eigen::MatrixXd toMatrixXd() const;           // convenience upcast

    // Conversion
    static MlTensor fromRawBuffer(const float* data, int rows, int cols);

private:
    Eigen::MatrixXf m_data;
    QVector<int> m_shape;
};
```

### Acceptance criteria
- **Data-driven training test**: Extract band-power features (alpha, beta) from
  real M/EEG epochs in `mne-cpp-test-data/` (e.g. auditory evoked data), train
  `MlLinearModel` (ridge regression) to predict stimulus condition, achieve
  R² > 0.5 on held-out fold (realistic for 2-class M/EEG).
- **Logistic regression test**: Train on synthetic 2-class data where classes
  are linearly separable with Gaussian noise; accuracy > 90%.
- **Ridge regression test**: Train on synthetic linear trend with noise;
  R² > 0.9.
- `MlLinearModel::save("model.onnx")` produces a valid ONNX file loadable by
  `MlOnnxModel::load()` (when ONNX Runtime is available).
- Round-trip: train → save → load → predict gives identical results
  (max difference < 1e-6).
- `MlPipeline` (scaler + model) produces identical results to manual
  scale-then-predict.
- `MlScaler` z-score: output mean ≈ 0, std ≈ 1 (tolerance 1e-10).
- Builds successfully with `USE_ONNXRUNTIME=OFF` (ONNX classes are stubs;
  `MlOnnxModel` methods throw with clear message).
- **Edge cases**: empty tensor, single-sample input, NaN in input (should not
  crash, should return error or NaN output).
- **Coverage**: ≥75% line coverage for all `ml` library source files.

---

## TASK 3 — New Library: `stats` (Statistical Testing)

### Goal
Create the `stats` library (`src/libraries/stats/`) providing non-parametric
cluster permutation tests, parametric t/F-tests, and multiple-comparison
corrections (FDR, Bonferroni). This is the #1 most-used MNE-Python feature that
mne-cpp completely lacks (see gap-analysis §8).

### Why
Every neuroscience paper requires statistical testing of M/EEG contrasts.
Cluster-based permutation testing (Maris & Oostenveld, 2007) is the gold standard
for spatio-temporal data because it controls family-wise error rate without
assuming independence of time/space points. Without this library, mne-cpp users
must export data to Python/R for statistics.

### Files to create

| File | Purpose |
|------|---------|
| `stats_global.h` | Export macro `STATSSHARED_EXPORT` |
| `stats_global.cpp` | Build-info stamp |
| `stats_types.h` | Enums: `StatsTailType` (Left, Right, Both), `StatsCorrection` (None, Bonferroni, Fdr, ClusterPermutation) |
| `stats_ttest.h/.cpp` | `StatsTtest` — one-sample, paired, independent t-tests (parametric); returns t-statistics + p-values as Eigen matrices |
| `stats_ftest.h/.cpp` | `StatsFtest` — one-way and repeated-measures F-test (ANOVA); returns F-statistics + p-values |
| `stats_correction.h/.cpp` | `StatsCorrection` — FDR (Benjamini-Hochberg), Bonferroni, Holm-Bonferroni; operates on p-value matrices |
| `stats_cluster.h/.cpp` | `StatsCluster` — spatio-temporal cluster permutation test (the core feature) |
| `stats_adjacency.h/.cpp` | `StatsAdjacency` — build spatial adjacency matrices from `FiffInfo` channel positions or source space tessellations |
| `CMakeLists.txt` | Build config (depends on `mne_utils`, `mne_math`, `mne_fiff`, `Qt6::Core`, `eigen`) |

### Class design: `StatsCluster`

This is the most important class. It implements the Maris & Oostenveld (2007)
cluster-based permutation test for 2D data (channels × time or vertices × time).

```
struct STATSSHARED_EXPORT StatsClusterResult
{
    Eigen::MatrixXd  matTObs;          // Observed t-statistic map (ch × time)
    QVector<double>  vecClusterStats;  // Sum-t for each significant cluster
    QVector<double>  vecClusterPvals;  // p-value for each cluster
    Eigen::MatrixXi  matClusterIds;    // Cluster label map (ch × time); 0=no cluster
    double           clusterThreshold; // The t-threshold used for forming clusters
};

class STATSSHARED_EXPORT StatsCluster
{
public:
    // Main entry point: cluster permutation t-test between two conditions
    // matA: condition A data (n_observations × n_channels × n_times), stored
    //       as a QVector of Eigen::MatrixXd (one matrix per observation)
    // matB: condition B data (same shape)
    // adjacency: sparse boolean adjacency matrix (n_channels × n_channels)
    //            from StatsAdjacency
    //
    // Returns cluster result with significant clusters at the given alpha level.
    static StatsClusterResult permutationTest(
        const QVector<Eigen::MatrixXd>& dataA,
        const QVector<Eigen::MatrixXd>& dataB,
        const Eigen::SparseMatrix<int>& adjacency,
        int    nPermutations = 1024,
        double clusterAlpha  = 0.05,
        double pThreshold    = 0.05,
        StatsTailType tail   = StatsTailType::Both);

private:
    // Compute t-statistic map from two groups
    static Eigen::MatrixXd computeTMap(
        const QVector<Eigen::MatrixXd>& dataA,
        const QVector<Eigen::MatrixXd>& dataB);

    // Find connected clusters above threshold in t-map using adjacency
    static QPair<Eigen::MatrixXi, QVector<double>> findClusters(
        const Eigen::MatrixXd& tMap,
        double threshold,
        const Eigen::SparseMatrix<int>& adjacency,
        StatsTailType tail);

    // One permutation iteration: shuffle labels, compute max cluster stat
    static double permuteOnce(
        const QVector<Eigen::MatrixXd>& allData,
        int nA,
        const Eigen::SparseMatrix<int>& adjacency,
        double threshold,
        StatsTailType tail);
};
```

### Algorithm (for the implementing agent)

1. **Observed t-map**: Compute paired or independent t-statistic at each
   (channel, time) point from `dataA` vs `dataB`.
2. **Threshold**: Find all (channel, time) points where |t| > threshold
   (derived from `clusterAlpha` via inverse t-distribution or direct threshold).
3. **Cluster formation**: Use breadth-first-search on the adjacency graph ⊗ time
   adjacency (consecutive time points are always adjacent) to find connected
   components of supra-threshold points.
4. **Cluster statistic**: Sum the t-values within each cluster.
5. **Permutation distribution**: Repeat `nPermutations` times:
   a. Randomly reassign observations to groups A/B.
   b. Compute t-map → clusters → take the maximum cluster sum-t.
   c. Store in null distribution.
6. **p-values**: For each observed cluster, p = (# null ≥ observed) / nPermutations.
7. **Parallelise** permutations using `QtConcurrent::mappedReduced` or
   `#pragma omp parallel for`.

### `StatsAdjacency`
```
class STATSSHARED_EXPORT StatsAdjacency
{
public:
    // From sensor positions (Delaunay triangulation of channel locations)
    static Eigen::SparseMatrix<int> fromChannelPositions(
        const FIFFLIB::FiffInfo& info,
        const QStringList& picks = QStringList());

    // From source space triangulation (vertices are adjacent if they share a triangle edge)
    static Eigen::SparseMatrix<int> fromSourceSpace(
        const Eigen::MatrixX3i& tris,
        int nVertices);
};
```

### Acceptance criteria
- **Data-driven cluster test**: Load real evoked data from `mne-cpp-test-data/`
  (auditory left vs right), build adjacency from `FiffInfo` channel positions,
  run cluster permutation test. Known auditory contrast should produce at least
  one significant cluster (p < 0.05) in the temporal region around 100 ms.
- **Synthetic cluster test**: Generate 20 "subjects" × 64 channels × 100 time
  points with a planted Gaussian blob (10 adjacent channels, 20 time points,
  effect size d=1.0). Cluster test must find the blob (p < 0.01) and reject
  noise regions (p > 0.1).
- **t-test validation**: Paired t-test on known data matches SciPy
  `scipy.stats.ttest_rel()` output to 6 decimal places.
- **F-test validation**: One-way F-test matches SciPy `scipy.stats.f_oneway()`.
- **FDR validation**: Benjamini-Hochberg correction on a known p-value vector
  matches `statsmodels.stats.multitest.multipletests(method='fdr_bh')`.
- **Bonferroni**: trivial check: corrected_p = min(p * n, 1.0).
- **Adjacency**: Matrix from `FiffInfo` with VectorView-306 layout is symmetric,
  sparse (~6–10 neighbors per channel), and has correct dimensions (306×306 or
  n_picks × n_picks).
- Permutation loop runs in parallel (wall-clock < 50% of serial time on ≥2
  cores for 1024 permutations).
- **Edge cases**: single observation (should fail gracefully), single channel
  (no adjacency needed), all-zero data.
- **Coverage**: ≥75% line coverage for all `stats` library source files.

---

## TASK 4 — FiffAnnotations Class

### Goal
Add a library-level `FiffAnnotations` class to `src/libraries/fiff/` that stores
time-stamped annotation segments (onset, duration, description, channels) for raw
data. This is the equivalent of MNE-Python's `mne.Annotations`.

### Why
mne-cpp's `AnnotationModel` in `src/applications/mne_browse/Models/annotationmodel.h`
already has the right data structure (`AnnotationEntry` with startSample, endSample,
description, channelNames, comment, extras) but it is an application-level
`QAbstractTableModel` with `private` data. Other applications (MNE Analyze, MNE
Scan, CLI tools) cannot use it. We need a clean library-level container that the
mne_browse model can then wrap.

### Existing code to refactor
`src/applications/mne_browse/Models/annotationmodel.h` contains:
- `struct AnnotationEntry { startSample, endSample, description, channelNames, comment, extras }`
- `loadAnnotationData()` / `saveAnnotationData()` supporting JSON, FIF, CSV, TXT

The library class extracts the data + I/O; the application model becomes a thin
Qt wrapper around `FiffAnnotations`.

### Files to create/modify

| File | Action | Purpose |
|------|--------|---------|
| `src/libraries/fiff/fiff_annotations.h` | CREATE | `FiffAnnotations` class declaration |
| `src/libraries/fiff/fiff_annotations.cpp` | CREATE | Implementation |
| `src/libraries/fiff/CMakeLists.txt` | MODIFY | Add new files to SOURCES/HEADERS |
| `src/applications/mne_browse/Models/annotationmodel.h` | MODIFY | Replace `AnnotationEntry` with `FiffAnnotations` usage |
| `src/applications/mne_browse/Models/annotationmodel.cpp` | MODIFY | Delegate data storage + I/O to `FiffAnnotations` |

### Class design: `FiffAnnotations`

```
namespace FIFFLIB
{

struct FIFFSHARED_EXPORT FiffAnnotation
{
    double      onset;          // Onset in seconds (relative to meas_date or first_samp)
    double      duration;       // Duration in seconds (0 for point events)
    QString     description;    // Label: "BAD", "BAD_ACQ_SKIP", "EDGE", custom...
    QStringList channelNames;   // Empty = global annotation; non-empty = channel-specific
    QString     comment;        // Optional free-form comment
    QVariantMap extras;         // Format-specific metadata for round-tripping
};

class FIFFSHARED_EXPORT FiffAnnotations
{
public:
    typedef QSharedPointer<FiffAnnotations> SPtr;
    typedef QSharedPointer<const FiffAnnotations> ConstSPtr;

    FiffAnnotations();

    // Element access
    int size() const;
    bool isEmpty() const;
    const FiffAnnotation& operator[](int index) const;
    FiffAnnotation& operator[](int index);

    // Modification
    void append(const FiffAnnotation& annotation);
    void append(double onset, double duration, const QString& description,
                const QStringList& channelNames = QStringList(),
                const QString& comment = QString());
    void remove(int index);
    void clear();

    // Bulk access
    const QVector<FiffAnnotation>& toVector() const;

    // Sample conversion helpers (require sfreq)
    int onsetToSample(int index, double sfreq, int firstSample = 0) const;
    int endToSample(int index, double sfreq, int firstSample = 0) const;

    // Query
    FiffAnnotations select(const QString& descriptionFilter) const;  // e.g. "BAD"
    FiffAnnotations selectByChannel(const QString& channelName) const;
    FiffAnnotations crop(double tmin, double tmax) const;

    // I/O — JSON sidecar (*_annotations.json)
    static FiffAnnotations readJson(const QString& path);
    static bool writeJson(const QString& path, const FiffAnnotations& annot);

    // I/O — CSV (*_annotations.csv: onset,duration,description)
    static FiffAnnotations readCsv(const QString& path);
    static bool writeCsv(const QString& path, const FiffAnnotations& annot);

    // I/O — FIFF (embedded in raw file, FIFF_ANNOTATION block)
    // These are called internally by FiffRawData read/write
    static FiffAnnotations readFromFiff(FiffStream::SPtr stream);
    static bool writeToFiff(FiffStream::SPtr stream, const FiffAnnotations& annot);

    // I/O — auto-detect format from file extension
    static FiffAnnotations read(const QString& path);
    static bool write(const QString& path, const FiffAnnotations& annot);

private:
    QVector<FiffAnnotation> m_annotations;
};

}  // namespace FIFFLIB
```

### Refactoring `AnnotationModel`
After `FiffAnnotations` exists, the mne_browse `AnnotationModel` changes to:
- Store a `FiffAnnotations` member instead of `QVector<AnnotationEntry>`.
- `addAnnotation()` calls `m_annotations.append(...)`.
- `loadAnnotationData()` calls `FiffAnnotations::read(path)`.
- `saveAnnotationData()` calls `FiffAnnotations::write(path)`.
- Remove the private `AnnotationEntry` struct.

### Acceptance criteria
- **Data-driven I/O test**: Create `FiffAnnotations` from the annotations
  embedded in a real raw FIFF file from `mne-cpp-test-data/` (if present) or
  from a hand-crafted JSON sidecar mimicking BIDS `*_events.json` structure
  with realistic BAD segments, stimulus events, and edge markers.
- `FiffAnnotations::readJson()` → `writeJson()` round-trips losslessly
  (byte-identical JSON output after re-read).
- `FiffAnnotations::readCsv()` → `writeCsv()` round-trips (field order and
  precision preserved).
- `select("BAD")` on a set with BAD_ACQ_SKIP, BAD_EOG, STIM, EDGE returns
  exactly the two BAD-prefixed entries.
- `selectByChannel("MEG0113")` returns only annotations that include that
  channel in their channelNames list.
- `crop(1.0, 5.0)` returns annotations whose [onset, onset+duration] interval
  overlaps [1,5]; annotations fully outside are excluded; partially
  overlapping annotations have their onset/duration clipped to the window.
- `onsetToSample()` and `endToSample()` with sfreq=1000, firstSample=0:
  annotation at onset=1.5s → sample 1500.
- mne_browse compiles and works identically after the refactor (manual
  verification: load a file, add annotation, save, reload, verify present).
- **Edge cases**: empty annotations object, annotation with duration=0 (point
  event), annotation at t=0, annotation with non-ASCII description.
- **Coverage**: ≥75% line coverage for `fiff_annotations.cpp`.

---

## TASK 5 — Multitaper PSD & TFR (DPSS)

### Goal
Add multitaper spectral estimation to `src/libraries/dsp/` — both static PSD and
time-frequency resolved (TFR). This fills a high-priority gap: Thomson's
multitaper method provides better spectral resolution than Welch with formal
statistical control via the bandwidth parameter.

### Why
Multitaper PSD is the standard in neuroscience for spectral analysis of M/EEG
data. It provides optimal bias-variance tradeoff controlled by the half-bandwidth
parameter. The existing `WelchPsd` class handles simple periodogram averaging but
does not support DPSS tapers or time-frequency resolution.

### Files to create

| File | Purpose |
|------|---------|
| `dpss.h/.cpp` | `Dpss` — compute Discrete Prolate Spheroidal Sequences (Slepian tapers). Static method: `Dpss::compute(int N, double halfBandwidth, int nTapers)` returns `Eigen::MatrixXd` (nTapers × N). Algorithm: solve the tridiagonal eigenvalue problem from Slepian (1978). |
| `multitaper_psd.h/.cpp` | `MultitaperPsd` — apply DPSS tapers, FFT each taper, average. Interface mirrors `WelchPsd`: static `compute(matData, sfreq, halfBandwidth, nTapers)` returns `MultitaperPsdResult { matPsd, vecFreqs }`. |
| `multitaper_tfr.h/.cpp` | `MultitaperTfr` — sliding-window multitaper: segment data into overlapping windows, apply DPSS within each window. Returns 3D result (channels × freqs × times). Store as `MultitaperTfrResult { QVector<Eigen::MatrixXd> tfrData, vecFreqs, vecTimes }`. |

### Modify
- `src/libraries/dsp/CMakeLists.txt`: add new files to SOURCES/HEADERS.

### Algorithm: DPSS computation
The DPSS are the eigenvectors of the N×N tridiagonal matrix:
```
T(i,i) = ((N-1-2i)/2)^2 * cos(2π * W)     for i = 0..N-1
T(i,i+1) = T(i+1,i) = i*(N-i)/2
```
where W = halfBandwidth/N. Use Eigen's `SelfAdjointEigenSolver` on the
tridiagonal matrix. Return the top `nTapers` eigenvectors (highest eigenvalues).
Eigenvalues (concentration ratios) should also be returned for adaptive weighting.

### Acceptance criteria
- **DPSS validation**: Tapers for N=256, W=4 match SciPy `scipy.signal.windows.dpss()`
  to <1e-10 relative error (pre-compute SciPy reference values and embed as
  test constants). Eigenvalues (concentration ratios) match to <1e-8.
- **Data-driven PSD test**: Load real MEG data from `mne-cpp-test-data/`,
  compute multitaper PSD, verify alpha peak (~10 Hz) is visible in posterior
  channels (peak frequency within 8–13 Hz band).
- **Synthetic PSD test**: Generate 10 Hz + 40 Hz sinusoids in white noise
  (SNR=10 dB). Multitaper PSD peaks must be at 10±1 Hz and 40±1 Hz.
- **TFR test**: Generate a linear chirp (10→50 Hz over 2s). Multitaper TFR
  must show monotonically increasing peak frequency over time (verify
  correlation > 0.95 between time index and peak frequency at each time bin).
- **Comparison with Welch**: Same data, multitaper PSD should have narrower
  main lobe than Welch PSD (measure -3dB bandwidth of the 10 Hz peak).
- **Performance**: PSD of 64 channels × 60s at 1000 Hz completes in <2s on
  a single core.
- **Edge cases**: single-channel input, very short window (N=16), nTapers=1
  (degenerates to single-taper periodogram).
- **Coverage**: ≥75% line coverage for `dpss.cpp`, `multitaper_psd.cpp`,
  `multitaper_tfr.cpp`.

---

## TASK 6 — Cross-Spectral Density (CSD)

### Goal
Add CSD computation to `src/libraries/dsp/` via Fourier, multitaper, or Morlet
methods. CSD is a prerequisite for improved DICS beamformer and spectral
connectivity.

### Files to create

| File | Purpose |
|------|---------|
| `csd.h/.cpp` | `Csd` class — static methods for cross-spectral density estimation |

### Class design
```
struct DSPSHARED_EXPORT CsdResult
{
    Eigen::MatrixXcd matCsd;           // n_channels × n_channels complex CSD at one freq
    QVector<Eigen::MatrixXcd> csdByFreq; // Full CSD: one matrix per frequency bin
    Eigen::RowVectorXd vecFreqs;       // Frequency axis
};

class DSPSHARED_EXPORT Csd
{
public:
    // Multitaper CSD (recommended)
    static CsdResult computeMultitaper(
        const Eigen::MatrixXd& matData,    // n_channels × n_times
        double sfreq,
        double fmin = 0.0,
        double fmax = -1.0,                // -1 → Nyquist
        double halfBandwidth = 4.0,
        int nTapers = -1);                 // -1 → 2*halfBandwidth - 1

    // Fourier CSD (Welch-style segmented)
    static CsdResult computeFourier(
        const Eigen::MatrixXd& matData,
        double sfreq,
        double fmin = 0.0,
        double fmax = -1.0,
        int nFft = 256,
        double overlap = 0.5);

    // Morlet CSD
    static CsdResult computeMorlet(
        const Eigen::MatrixXd& matData,
        double sfreq,
        const Eigen::RowVectorXd& frequencies,
        int nCycles = 7);
};
```

### Acceptance criteria
- **Data-driven DICS test**: Load real MEG data from `mne-cpp-test-data/`,
  compute multitaper CSD in the beta band (13–30 Hz), verify the CSD matrix
  is Hermitian and has plausible structure (diagonal dominance, coherent
  off-diagonal for nearby sensors).
- **White noise test**: CSD of 32-channel white noise (10000 samples) has
  off-diagonal magnitude < 5% of diagonal magnitude.
- **Correlated pair test**: Two channels with identical signal →
  |csd(0,1)| / |csd(0,0)| > 0.95.
- **Hermitian symmetry**: `(matCsd - matCsd.adjoint()).norm() < 1e-12`.
- **Frequency axis**: For sfreq=1000, nFft=256, frequency bins match 0, ~3.9,
  ~7.8, ..., 500 Hz (within tolerance).
- **Cross-method consistency**: CSD via multitaper and via Fourier (Welch)
  produce qualitatively similar results on the same data (correlation of
  diagonal elements > 0.9).
- **Edge cases**: single channel (CSD is real PSD), fmin > fmax (error), data
  shorter than nFft (error with clear message).
- **Coverage**: ≥75% line coverage for `csd.cpp`.

---

## TASK 7 — Extended Infomax ICA

### Goal
Add Extended Infomax ICA to `src/libraries/dsp/`. The existing `ica.h/.cpp`
implements FastICA (deflationary, logcosh). Extended Infomax handles both
super-Gaussian and sub-Gaussian source distributions, making it the default choice
in many EEG labs (e.g. EEGLAB's `runica`).

### Why
FastICA assumes all sources are super-Gaussian (pointed distributions). EEG data
often contains sub-Gaussian sources (e.g. line noise, flat distributions).
Extended Infomax automatically detects and adapts to both, producing more reliable
decompositions for artifact removal.

### Files to create

| File | Purpose |
|------|---------|
| `extended_infomax.h/.cpp` | `ExtendedInfomax` class |

### Class design
```
struct DSPSHARED_EXPORT InfomaxResult
{
    Eigen::MatrixXd matUnmixing;   // n_components × n_channels
    Eigen::MatrixXd matMixing;     // n_channels × n_components (pseudo-inverse)
    Eigen::MatrixXd matSources;    // n_components × n_times (optional, if requested)
    int nIterations;               // Actual iterations used
    bool converged;                // Whether convergence criterion was met
};

class DSPSHARED_EXPORT ExtendedInfomax
{
public:
    static InfomaxResult compute(
        const Eigen::MatrixXd& matData,         // n_channels × n_times (mean-removed)
        int nComponents = -1,                     // -1 → n_channels
        int maxIterations = 200,
        double learningRate = 0.001,
        double tolerance = 1e-7,
        bool extendedMode = true,                // true = extended (sub+super Gaussian)
        unsigned int seed = 0);                  // 0 = random seed

private:
    // Learning rule sign vector: +1 for super-Gaussian, -1 for sub-Gaussian
    // Updated every block using kurtosis estimate
    static Eigen::VectorXd estimateSignVector(const Eigen::MatrixXd& matSources);
};
```

### Algorithm (for the implementing agent)
1. **Whitening**: PCA → whitening transform (same as FastICA preprocessing).
2. **Initialize** weights W as identity (or small random perturbation).
3. **For each iteration**:
   a. Compute sources: `S = W * X_white`.
   b. Every N samples, estimate kurtosis of each source to get sign vector
      `k`: `k(i) = +1` if excess kurtosis > 0 (super-Gaussian),
      `k(i) = -1` if < 0 (sub-Gaussian).
   c. Compute natural gradient update:
      `dW = (I - k * tanh(S) * S^T - S * S^T) * W`
      (the `k` vector selects the nonlinearity per component).
   d. `W = W + learningRate * dW`.
   e. Check convergence: `max(abs(dW)) < tolerance`.
4. **De-whiten** to get mixing/unmixing in original sensor space.

### Acceptance criteria
- **Data-driven artifact test**: Load real MEG data from `mne-cpp-test-data/`,
  run Extended Infomax with 20 components, verify that at least one component
  correlates with the ECG channel (r > 0.3) — demonstrating real artifact
  separation.
- **Synthetic super-Gaussian test**: Mix 3 Laplacian sources through a random
  mixing matrix. Extended Infomax unmixing recovers all 3 sources
  (correlation > 0.95 after Amari-index-based permutation alignment).
- **Synthetic sub-Gaussian test**: Mix 3 uniform-distribution sources.
  Extended Infomax recovers them (correlation > 0.90). FastICA alone would
  fail on this (can optionally verify as a comparison).
- **Mixed test**: 2 super-Gaussian + 1 sub-Gaussian source. Extended Infomax
  handles both (all correlations > 0.90). Verify the sign vector correctly
  identifies the sub-Gaussian component.
- **Convergence**: On 64-channel × 60s simulated EEG, converges within 200
  iterations (flag `converged` is true).
- Existing `ICA` class (FastICA in `ica.h`) is not modified — zero changes.
- **Determinism**: With fixed seed, two runs produce identical unmixing matrices.
- **Edge cases**: n_components > n_channels (should fail gracefully),
  single-channel input, constant-value channel (should warn or skip).
- **Coverage**: ≥75% line coverage for `extended_infomax.cpp`.

---

## TASK 8 — Sparse Inverse Methods (MxNE + Gamma-MAP)

### Goal
Add Mixed-Norm Estimate (MxNE) and Gamma-MAP sparse inverse solvers to
`src/libraries/inv/`. These find focal (spatially sparse) source distributions —
complementing the existing distributed (L2) inverse methods.

### Where
Create a new subdirectory `src/libraries/inv/sparse/` alongside the existing
`minimum_norm/`, `beamformer/`, `dipole_fit/`, `rap_music/`, `hpi/`.

### Files to create

| File | Purpose |
|------|---------|
| `sparse/inv_mxne.h/.cpp` | `InvMxne` — L1/L2 mixed-norm (MxNE) solver |
| `sparse/inv_gamma_map.h/.cpp` | `InvGammaMap` — hierarchical Bayesian sparse inverse |

### Class design: `InvMxne`
```
namespace INVLIB {

struct INVSHARED_EXPORT InvMxneResult
{
    InvSourceEstimate stc;        // Sparse source estimate (only active vertices have non-zero)
    QVector<int> activeVertices;  // Indices of selected active sources
    int nIterations;              // IRLS iterations used
    double residualNorm;          // Final data-fit residual
};

class INVSHARED_EXPORT InvMxne
{
public:
    // matGain: n_channels × n_sources (forward operator, already whitened)
    // matData: n_channels × n_times (whitened evoked/epochs data)
    // alpha: regularisation strength (L1 penalty)
    // nIterations: max IRLS iterations
    // tolerance: convergence criterion on weight change
    static InvMxneResult compute(
        const Eigen::MatrixXd& matGain,
        const Eigen::MatrixXd& matData,
        double alpha,
        int nIterations = 50,
        double tolerance = 1e-6);
};

}
```

### Algorithm: MxNE (for the implementing agent)
MxNE minimizes: `||M - G*X||²_F + α * Σ_i ||X_i||_2`
where `X_i` is the i-th source's time course (row of X), and `||·||_2` is the
L2 (Euclidean) norm of that row. This is a group-lasso / L21-norm penalty.

Solve via **Iterative Reweighted Least Squares (IRLS)**:
1. Initialize weights `w_i = 1` for all sources.
2. For each iteration:
   a. Form diagonal weight matrix `W = diag(w_1, ..., w_n)`.
   b. Solve weighted ridge: `X = (G^T G + α W^{-2})^{-1} G^T M`.
   c. Update weights: `w_i = max(||X_i||_2, ε)` (ε = 1e-10 for stability).
   d. Prune sources where `w_i < ε_prune` (active set strategy for speed).
   e. Check convergence: `max |w_new - w_old| < tolerance`.
3. Return sparse `X` with only active sources.

### Algorithm: Gamma-MAP (for the implementing agent)
Gamma-MAP places a Gamma hyperprior on source variances:
1. Initialize source variances `γ_i = 1`.
2. E-step: Compute posterior mean `X = Σ_post G^T Σ_noise^{-1} M` where
   `Σ_post = (G^T Σ_noise^{-1} G + diag(1/γ))^{-1}`.
3. M-step: Update `γ_i = ||X_i||²_2 / T` where T is number of time points.
4. Prune sources where `γ_i < threshold`.
5. Repeat until convergence.

### Acceptance criteria
- **Data-driven MxNE test**: Load the forward solution and noise covariance from
  `mne-cpp-test-data/`. Generate synthetic evoked data by placing 2 known focal
  sources (e.g. vertices 1000 and 5000 on the left hemisphere) with distinct
  time courses, project through the forward operator, add realistic sensor
  noise from the noise covariance. Run MxNE and verify:
  - Both sources are recovered (peak vertex within 5 mm of true location).
  - Crosstalk < 5% (each recovered source's waveform correlates < 0.05 with
    the other true source's waveform).
  - Active set size < 20 vertices (sparse).
- **Gamma-MAP test**: Same setup. Verify convergence (iterations < 100),
  sparse output, and source localisation accuracy comparable to MxNE.
- **Active set shrinkage**: Log active set size per iteration; verify
  monotonic decrease after iteration 3.
- **STC compatibility**: `InvMxneResult.stc` writes to disk via existing
  `InvSourceEstimate::write()` and reads back identically.
- **Regularisation sweep**: Run MxNE with alpha=0.01, 0.1, 1.0, 10.0. Verify
  that higher alpha produces fewer active sources (monotonic relationship).
- **Edge cases**: single time point, alpha=0 (should degenerate to MNE-like
  solution), forward matrix with collinear columns (should not crash).
- **Coverage**: ≥75% line coverage for `inv_mxne.cpp` and
  `inv_gamma_map.cpp`.

---

## TASK 9 — Contextual MNE (CMNE)

### Publication
> Dinh C, Samuelsson JG, Hunold A, Hämäläinen MS, Khan S.
> **Contextual MEG and EEG Source Estimates Using Spatiotemporal LSTM Networks.**
> Front. Neurosci. 2021;15:552666.
> DOI: [10.3389/fnins.2021.552666](https://doi.org/10.3389/fnins.2021.552666)

### Reference codebase
The Python reference implementation lives at
[github.com/chdinh/cmne](https://github.com/chdinh/cmne) (MIT license).
Key files in the reference repo:
- `cmne/data.py` — `Data` class, `generate_lstm_batches()`, `standardize()`,
  `reshape_stc_data()`. Uses `mne.minimum_norm.apply_inverse_epochs` to
  compute dSPM source estimates, then z-scores and reshapes into
  `(look_back, n_sources)` sliding windows for LSTM training.
- `cmne/train.py` — `train()` builds a Keras `Sequential` model:
  `LSTM(num_units=1280, tanh, input_shape=(80, n_sources))` →
  `Dense(n_sources, linear)`. Compiled with MSE loss + Adam optimiser.
  Trains via `fit_generator` with minibatch_size=30, steps_per_ep=25.
- `cmne/settings.py` — `Settings` class holding ASSR data paths
  (`assr_270LP_fs900_raw.fif`, `*-ico-4-meg-eeg-inv.fif`, `*-eve.fif`).
- `examples/ex_cmne.py` — Full CMNE pipeline: loads data, trains LSTM,
  then applies the Markov-chain contextual correction loop:
  ```
  for i in range(steps):
      stc_prior = stc_result[i:i+lb]
      stc_predict = lstm_model.predict(stc_prior)
      stc_posterior = stc_sens[i+lb] * stc_predict   # elementwise
      stc_result[i+lb] = stc_posterior
  ```
  Also computes a control estimate (80-sample moving average × dSPM).

### Goal
Implement CMNE in `src/libraries/inv/minimum_norm/`. CMNE uses a
spatiotemporal LSTM network to predict the next source estimate from a
sequence of past estimates, then multiplies (element-wise) this prediction
with the current dSPM estimate to produce a context-corrected source map.
Unlike standard MNE/dSPM which operates sample-by-sample, CMNE exploits the
temporal structure of brain activity to improve spatial fidelity.

The ONNX Runtime integration from the `ml` library (TASK 2) provides the LSTM
inference engine; training is done offline in Python (reference repo above).

### Mathematical formulation (from the paper)

**Standard dSPM (time-invariant, per-sample)**:

1. Whitened data: `ỹ_t = C_n^{-1/2} y_t`
2. Whitened gain: `G̃ = C_n^{-1/2} G`
3. MNE kernel: `K = C_R G̃^T (G̃ C_R G̃^T + λ²I)^{-1}`
4. dSPM normalisation: `K_dSPM = W_dSPM K` where
   `W_dSPM(i,i) = 1 / sqrt(diag_i(K C_n K^T))`
5. Estimate: `x̂_t = K_dSPM ỹ_t`
6. Rectify + z-score: `q̂_t(i) = (|x̂_t(i)| - μ) / σ`

**CMNE correction (time-dependent, recursive)**:

7. For `t < k` (look-back window): `b_t = q̂_t` (identity — no correction)
8. For `t ≥ k`:
   a. Feed `{b_{t-k}, ..., b_{t-1}}` into the LSTM network.
   b. LSTM output → `|LSTM(...)| / max(|LSTM(...)|)` → diagonal of `W_t^CMNE`.
   c. CMNE estimate: `b_t = W_t^CMNE q̂_t` (element-wise product).
   d. `b_t` replaces `q̂_t` in the sliding window (Markov chain).

This forms a recursive Markov chain where the CMNE estimate itself is used as
input to the LSTM for subsequent predictions (Eq. 13 in the paper).

**Hyperparameters** (from the paper's cross-validation):
- `k = 80` — LSTM look-back (number of past time steps)
- `d = 1280` — number of hidden units in the LSTM layer
- Network: single LSTM layer → single Dense(n_sources, linear) output layer
- Loss: MSE, Optimiser: Adam
- Training: 85% epochs train / 15% validation, minibatch=30, 30 windows/batch

**Performance metrics** (from the paper):
- Peak localisation error: `PE = ||r_i - r_j||_2`
- Spatial dispersion: `SD = Σ_k d_jk |x̂_k| / Σ_k |x̂_k|`
- Source-space SNR: `SNR = σ²(active, signal) / σ²(active, noise)`

### Files to create/modify

| File | Action | Purpose |
|------|--------|---------|
| `minimum_norm/inv_cmne.h/.cpp` | CREATE | `InvCmne` solver class — the CMNE spatiotemporal correction |
| `minimum_norm/inv_cmne_settings.h/.cpp` | CREATE | `InvCmneSettings` — hyperparameters + ONNX model path |
| `minimum_norm/CMakeLists.txt` or parent `CMakeLists.txt` | MODIFY | Add new files, link `mne_ml` for ONNX inference |

### Class design
```
struct INVSHARED_EXPORT InvCmneSettings
{
    QString onnxModelPath;          // Path to trained LSTM model (.onnx)
    int lookBack = 80;              // k: number of past time steps
    int numSources = 5124;          // n_s: must match model input dim
    double lambda2 = 1.0 / 9.0;    // Tikhonov regularisation
    int method = 1;                 // 0=MNE, 1=dSPM (default), 2=sLORETA
    double looseOriConstraint = 0.2;// Orientation constraint (0=fixed, 1=free)
};

struct INVSHARED_EXPORT InvCmneResult
{
    InvSourceEstimate stcDspm;       // Uncorrected dSPM estimate
    InvSourceEstimate stcCmne;       // CMNE-corrected estimate
    InvSourceEstimate stcLstmPredict;// Raw LSTM prediction (for diagnostics)
    Eigen::MatrixXd matKernelDspm;   // Static dSPM kernel (n_sources × n_channels)
};

class INVSHARED_EXPORT InvCmne
{
public:
    // Full CMNE pipeline:
    // 1. Compute dSPM estimates for all time samples (static kernel)
    // 2. Z-score + rectify the dSPM estimates
    // 3. Run the LSTM prediction loop (Markov chain)
    // 4. Multiply prediction × dSPM element-wise
    //
    // matEvoked: (n_channels × n_times) — evoked or single-trial data
    // matGain: (n_channels × n_sources) — forward operator
    // matNoiseCov: (n_channels × n_channels) — noise covariance
    // matSrcCov: (n_sources × n_sources) — source covariance (diagonal)
    // settings: hyperparameters + model path
    static InvCmneResult compute(
        const Eigen::MatrixXd& matEvoked,
        const Eigen::MatrixXd& matGain,
        const Eigen::MatrixXd& matNoiseCov,
        const Eigen::MatrixXd& matSrcCov,
        const InvCmneSettings& settings);

    // Lower-level: apply the LSTM correction loop given pre-computed dSPM
    // stc data. This is the inner Markov chain (Eq. 10–13).
    // matDspmData: (n_sources × n_times) — z-scored rectified dSPM
    // Returns: (n_sources × n_times) — CMNE corrected data
    static Eigen::MatrixXd applyLstmCorrection(
        const Eigen::MatrixXd& matDspmData,
        const QString& onnxModelPath,
        int lookBack);

private:
    // Compute the static dSPM kernel (Eqs. 5–8 in the paper)
    static Eigen::MatrixXd computeDspmKernel(
        const Eigen::MatrixXd& matGain,
        const Eigen::MatrixXd& matNoiseCov,
        const Eigen::MatrixXd& matSrcCov,
        double lambda2);

    // Z-score and rectify source estimates (Eq. 9)
    static Eigen::MatrixXd zScoreRectify(
        const Eigen::MatrixXd& matStcData);
};
```

### Algorithm (for the implementing agent)

Reference: `examples/ex_cmne.py` in the `chdinh/cmne` repository.

1. **Compute static dSPM kernel** (Eqs. 5–8):
   a. Whiten gain matrix: `G̃ = C_n^{-1/2} G`.
   b. MNE kernel: `K = C_R G̃^T (G̃ C_R G̃^T + λ²I)^{-1}`.
   c. dSPM normalisation: for each source `i`,
      `w_i = 1.0 / sqrt((K C_n K^T)(i,i))`. Form diagonal `W_dSPM`.
   d. `K_dSPM = W_dSPM * K`.
   Validate dimensions: K_dSPM is `(n_sources × n_channels)`.
   Cross-check with MNE-Python: `mne.minimum_norm.make_inverse_operator()`
   + `prepare_inverse_operator()` compute the same kernel. MNE-SVN C:
   `mne_inverse_util.c` → `mne_prepare_inverse_operator()`.

2. **Apply dSPM to all time samples**:
   `x̂ = K_dSPM * ỹ` → matrix `(n_sources × n_times)`.

3. **Rectify + z-score** (Eq. 9):
   For each source `i`: `q̂(i,:) = (|x̂(i,:)| - mean(|x̂(i,:)|)) / std(|x̂(i,:)|)`.
   Reference: `cmne.standardize()` in `cmne/data.py`.

4. **LSTM prediction loop** (Eqs. 10–13, Markov chain):
   a. Allocate result matrix `b` of shape `(n_sources × n_times)`.
   b. Copy first `k` columns from `q̂` into `b` (no correction for `t < k`).
   c. For `t = k` to `n_times - 1`:
      - Extract window: `input = b[:, t-k : t]` transposed to `(k, n_sources)`.
      - Run ONNX inference: `lstm_output = onnx_session.run(input)` → `(n_sources,)`.
      - Normalise: `w = |lstm_output| / max(|lstm_output|)`.
      - CMNE estimate: `b[:, t] = w .* q̂[:, t]` (element-wise product).
   Reference: the for-loop in `examples/ex_cmne.py` lines `stc_posterior = stc_sens[i+lb,:] * stc_predict`.

5. **Output**: Return both the corrected `b` and the uncorrected `q̂` as
   `InvSourceEstimate` objects.

### ONNX model preparation
The LSTM model is trained in Python using the `cmne` package (reference repo).
The trained Keras `.h5` model must be exported to ONNX format:
```python
import tf2onnx
model = load_model('model_meg-eeg_nu_1280_lb_80.h5')
onnx_model, _ = tf2onnx.convert.from_keras(model)
onnxruntime.save(onnx_model, 'cmne_lstm_1280_80.onnx')
```
The `.onnx` file is loaded at runtime by `InvCmne::compute()` via the `ml`
library's ONNX Runtime session (TASK 2). The model expects input shape
`(1, k, n_sources)` and outputs `(1, n_sources)`.

### Acceptance criteria
- **Data-driven ASSR-like test**: Using the forward solution and inverse operator
  from `mne-cpp-test-data/MEG/sample/`:
  - Load `sample_audvis_trunc-meg-eeg-oct-4-fwd.fif` and
    `sample_audvis_trunc-meg-eeg-oct-4-meg-inv.fif`.
  - Apply dSPM to `sample_audvis_trunc-ave.fif` evoked data (SNR=3, λ²=1/9).
  - Verify dSPM output range matches MNE-Python reference: `7.5 < max < 15`
    (from `mne/minimum_norm/tests/test_inverse.py`).
  - Apply the CMNE LSTM correction loop with a small test ONNX model
    (can be a randomly initialised model for CI — the point is to verify the
    pipeline mechanics, not the prediction quality).
  - Verify CMNE output has the same shape as dSPM output.
  - Verify the first `k` time steps of CMNE equal the dSPM (identity for `t < k`).
  - Verify CMNE values are non-negative (rectified input × non-negative weights).
- **Markov chain test**: Feed a synthetic `(n_sources × n_times)` dSPM matrix
  with a known pattern (e.g. single active source). Run the LSTM loop.
  Verify that the output for `t >= k` differs from the input (= the LSTM
  prediction is actually being applied), and that the sliding window is updated
  with the CMNE output (Markov property), not the original dSPM.
- **Control estimate comparison**: Implement the control estimate from the paper
  (80-sample moving average × dSPM) alongside CMNE. Verify that CMNE and
  control produce different results (LSTM adds non-linear context).
- **dSPM kernel correctness**: Verify `computeDspmKernel()` output matches
  MNE-Python's `K` matrix for the same forward/noise-cov/source-cov inputs,
  within `rtol=1e-5`. Cross-validate with MNE-SVN C `mne_inverse_util.c`.
- **z-score + rectify**: Verify `zScoreRectify()` produces zero-mean, unit-std
  output for each source across time. Cross-validate with
  `cmne.standardize()` from the reference Python repo.
- **STC compatibility**: Output writes and reads via existing STC I/O.
- **Performance metrics**: Implement the three paper metrics (PE, SD, SNR) as
  utility functions and verify they produce correct values on synthetic data
  with known ground truth (e.g. single dipole at known location).
- **Edge cases**: fewer than `k` time samples (should return dSPM only, no
  LSTM correction), lambda2=0 (should warn and use small default),
  ONNX model not found (should throw with clear error message).
- **Never skip**: If the ONNX model is unavailable in CI, create a minimal
  test model programmatically (or provide a small one in test data) — do NOT
  skip the test.
- **Coverage**: ≥75% line coverage for `inv_cmne.cpp` and `inv_cmne_settings.cpp`.

---

## TASK 10 — Stereotactic Depth Electrode (sEEG) Visualization

### Goal
Add **stereotactic depth electrode** (sEEG) rendering to `src/libraries/disp3D/`
and integrate it into MNE Inspect (`src/applications/mne_inspect/`). Each
electrode shaft is rendered as a 3D cylinder with individually labeled and
selectable contacts.

**Important**: This task is specifically about **stereotactic EEG (sEEG) depth
electrodes** — thin multi-contact shafts surgically implanted deep into the
brain, typically for epilepsy monitoring or research. These are NOT scalp EEG
electrodes, ECoG grid/strip electrodes, or DBS leads (though the rendering
infrastructure may later be extended to those). The class names, documentation,
and UI labels must make this distinction clear (e.g. `ElectrodeObject` with
`ElectrodeShaft` — the "shaft" terminology is specific to depth electrodes;
scalp EEG would use different geometry entirely).

### Why
sEEG (stereo-EEG) is a rapidly growing modality in epilepsy and cognitive
neuroscience. Stereotactic depth electrodes are surgically implanted through
burr holes to reach deep brain structures (hippocampus, amygdala, insula, etc.)
that are inaccessible to surface recordings. The existing BIDS library already
reads `*_electrodes.tsv` and supports `FIFFV_SEEG_CH` constants, but there is
no way to visualise these depth electrodes in 3D alongside brain surfaces.

### Files to create/modify

| File | Action | Purpose |
|------|--------|---------|
| `disp3D/renderable/electrodeobject.h/.cpp` | CREATE | `ElectrodeObject` — renderable depth electrode with instanced contacts |
| `disp3D/shaders/electrode.vert` | CREATE | Vertex shader: cylinder + sphere instancing |
| `disp3D/shaders/electrode.frag` | CREATE | Fragment shader: per-contact coloring, selection highlight |
| `disp3D/CMakeLists.txt` | MODIFY | Add new files |
| `mne_inspect` app files | MODIFY | Add electrode loading panel + render integration |

### Class design: `ElectrodeObject`
```
namespace DISP3DLIB {

struct ElectrodeContact
{
    QString name;           // e.g. "LH1", "LH2"
    QVector3D position;     // 3D position in MRI (surface RAS) coords
    float radius;           // Contact radius in mm (default 0.5)
    QColor color;           // Display color (can be data-driven)
    bool selected;          // Whether this contact is selected/highlighted
    float value;            // Optional scalar for colormap overlay (e.g. HFO rate)
};

struct ElectrodeShaft
{
    QString label;                      // Shaft label: "LH", "RA", etc.
    QVector<ElectrodeContact> contacts; // Ordered tip-to-tail
    float shaftRadius;                  // Cylinder radius in mm (default 0.4)
    QColor shaftColor;                  // Shaft body color (grey default)
};

class DISP3DSHARED_EXPORT ElectrodeObject
{
public:
    ElectrodeObject();

    // Load from BIDS electrodes
    void setFromBidsElectrodes(const QList<BIDSLIB::BidsElectrode>& electrodes,
                                const FIFFLIB::FiffCoordTrans& mriToHead);

    // Manual set
    void setShafts(const QVector<ElectrodeShaft>& shafts);

    // Per-contact data overlay (e.g. set color by iEEG amplitude)
    void setContactValues(const QMap<QString, float>& values,
                          const QColor& minColor = Qt::blue,
                          const QColor& maxColor = Qt::red);

    // Selection
    void selectContact(const QString& name);
    void clearSelection();

    // GPU buffer management (same pattern as DipoleObject)
    void initializeRhiResources(QRhi* rhi);
    void updateRhiResources(QRhiResourceUpdateBatch* batch);
    void releaseRhiResources();

    // Geometry access for the renderer
    int vertexCount() const;
    int indexCount() const;
    QRhiBuffer* vertexBuffer() const;
    QRhiBuffer* indexBuffer() const;
    QRhiBuffer* instanceBuffer() const;

private:
    QVector<ElectrodeShaft> m_shafts;
    // GPU buffers (same pattern as dipoleobject.h)
    // Cylinder mesh shared across all shafts; contacts are instanced spheres
};

}
```

### Rendering approach
- **Shaft cylinder**: Generate a unit cylinder mesh (e.g. 16 sides, 2 endcaps).
  For each shaft, compute the transform that stretches/rotates the unit cylinder
  from the first contact to the last contact position. Render as instanced draws.
- **Contact spheres**: Generate a unit sphere mesh (e.g. icosphere with 2
  subdivisions). Instance one sphere per contact with per-instance position,
  radius, and color. Use the existing instancing pattern from `DipoleObject`.
- **Shaders**: Similar to `dipole.vert`/`dipole.frag` — accept model matrix +
  per-instance transform. Fragment shader supports flat color, per-contact
  colormap, and selection highlight (pulsing emissive overlay).

### MNE Inspect integration
- Add an "Electrodes" panel to the MNE Inspect UI toolbar.
- "Load Electrodes" button → opens BIDS `*_electrodes.tsv` (uses
  `BidsElectrode::readTsv()`).
- Electrodes appear as a `ElectrodeObject` renderable in the scene alongside
  brain surfaces.
- Clicking a contact shows its name + coordinates in a tooltip.

### Acceptance criteria
- **Geometry test**: 10 shafts × 12 contacts each → verify vertex buffer has
  expected vertex count (cylinder: 16 sides × 2 endcaps + 120 sphere instances),
  index buffer is non-empty, bounding box encloses all contact positions.
- **BIDS electrode loading**: Load a synthetic `*_electrodes.tsv` with 120
  contacts across 10 shafts. Verify `ElectrodeObject::setFromBidsElectrodes()`
  produces the correct number of shafts and contacts with positions matching
  the TSV within 0.001 mm.
- **Contact selection**: `selectContact("LH5")` marks exactly one contact as
  selected. `clearSelection()` resets all.
- **Data overlay**: `setContactValues()` with known min/max values produces
  colors that interpolate correctly between minColor and maxColor.
- **Rendering performance**: 120 contacts render at >30 FPS alongside a full
  pial surface (measured on CI or documented as manual test).
- **Coordinate transform**: Electrode positions transformed through
  `mriToHead` match expected head-coordinate positions.
- **Edge cases**: empty electrode list, single contact (no shaft cylinder
  possible — should render just a sphere), duplicate contact names (warn).
- **Coverage**: ≥75% line coverage for `electrodeobject.cpp`.

---

## TASK 11 — MRI Volume Slicing

### Goal
Add orthogonal MRI slice extraction to `src/libraries/mri/` and a slice viewer
to `src/libraries/disp3D/` and MNE Inspect. The existing `MriVolData` class can
load MGH/MGZ volumes and compute the vox2ras transform. This task adds the ability
to extract axial/coronal/sagittal slices and render them as textured quads in the
3D scene.

### Files to create/modify

| File | Action | Purpose |
|------|--------|---------|
| `mri/mri_slicer.h/.cpp` | CREATE | `MriSlicer` — extract 2D slices from `MriVolData` along any axis at any position |
| `disp3D/renderable/sliceobject.h/.cpp` | CREATE | `SliceObject` — textured quad renderable for one MRI slice |
| `disp3D/shaders/slice.vert` | CREATE | Simple quad vertex shader with texture coordinates |
| `disp3D/shaders/slice.frag` | CREATE | Grayscale texture lookup with windowing (brightness/contrast) |
| `disp3D/CMakeLists.txt` | MODIFY | Add new files |
| `mri/CMakeLists.txt` | MODIFY | Add new files |

### Class design: `MriSlicer`
```
namespace MRILIB {

enum class SliceOrientation { Axial, Coronal, Sagittal };

struct MRISHARED_EXPORT MriSliceImage
{
    Eigen::MatrixXf pixels;         // 2D pixel data (width × height), normalised 0–1
    int width;
    int height;
    SliceOrientation orientation;
    int sliceIndex;                  // Voxel index along the slicing axis
    Eigen::Matrix4f sliceToRas;     // 4×4 transform placing the slice in RAS space
};

class MRISHARED_EXPORT MriSlicer
{
public:
    // Extract a single slice at the given voxel index along the given axis
    static MriSliceImage extractSlice(const MriVolData& vol,
                                       SliceOrientation orientation,
                                       int sliceIndex);

    // Extract all three orthogonal slices at a given RAS point
    static QVector<MriSliceImage> extractOrthogonal(const MriVolData& vol,
                                                      const Eigen::Vector3f& rasPoint);

    // Convert RAS coordinate to voxel index
    static Eigen::Vector3i rasToVoxel(const MriVolData& vol,
                                       const Eigen::Vector3f& rasPoint);

    // Convert voxel index to RAS coordinate
    static Eigen::Vector3f voxelToRas(const MriVolData& vol,
                                       const Eigen::Vector3i& voxel);
};

}
```

### `SliceObject` rendering
- A textured quad (2 triangles, 4 vertices) positioned in 3D space using the
  `sliceToRas` transform.
- Texture: upload `MriSliceImage::pixels` as a `QRhiTexture` (R32F or R8).
- Fragment shader: grayscale lookup with adjustable window/level.
- Support electrode overlay: render electrode positions as colored dots on the
  slice when they fall within ±1 voxel of the slice plane.

### MNE Inspect integration
- Three persistent slice objects (axial, coronal, sagittal) attached to a
  crosshair position.
- Crosshair controlled by: (a) click on brain surface → snap to nearest voxel,
  (b) scroll wheel → move slice index, (c) text input for RAS coordinates.
- Opacity slider for each slice plane.

### Acceptance criteria
- **Data-driven slice test**: Load a real T1 MGH/MGZ volume from
  `mne-cpp-test-data/` (or FreeSurfer sample subject). Extract axial slice at
  z=128 of a 256³ volume. Verify:
  - Slice dimensions match volume header (width × height = 256 × 256).
  - Pixel values are normalised to [0, 1].
  - Slice is not all-zero (contains brain anatomy).
- **Orthogonal extraction**: `extractOrthogonal()` at the corpus callosum RAS
  coordinate returns 3 slices with correct orientations.
- **RAS↔voxel round-trip**: For 100 random RAS points within the volume,
  `voxelToRas(rasToVoxel(ras))` returns the original RAS within 1 voxel
  tolerance (half-voxel rounding).
- **Transform accuracy**: Compare `computeVox2Ras()` output with FreeSurfer's
  `mri_info --vox2ras` on the same file (values match to <1e-5).
- **Performance**: Three slices update at >30 FPS during crosshair drag
  (render + texture upload).
- **Edge cases**: slice index out of bounds (clamp or return empty), volume
  with non-isotropic voxels, volume with nframes > 1.
- **Coverage**: ≥75% line coverage for `mri_slicer.cpp` and `sliceobject.cpp`.

---

## TASK 12 — Bipolar Re-Referencing (Channel Derivations)

### Goal
Add a channel derivation engine to `src/libraries/dsp/` that can compute bipolar
(and arbitrary linear) re-referenced channels from raw data. This is essential for
sEEG analysis where bipolar referencing suppresses far-field activity and improves
spatial resolution.

### Files to create

| File | Purpose |
|------|---------|
| `channel_derivation.h/.cpp` | `ChannelDerivation` — apply linear channel combinations |

### Class design
```
namespace UTILSLIB {

struct DSPSHARED_EXPORT DerivationRule
{
    QString outputName;                    // Name of derived channel (e.g. "LH1-LH2")
    QMap<QString, double> inputWeights;    // channel_name → weight (e.g. {"LH1": 1.0, "LH2": -1.0})
};

class DSPSHARED_EXPORT ChannelDerivation
{
public:
    // Build bipolar derivation rules for sequential contacts on each shaft.
    // channelNames: ordered list of all sEEG channel names.
    // Returns one rule per adjacent pair: ch[i] - ch[i+1].
    // Groups channels by shaft prefix (letters before the first digit).
    static QVector<DerivationRule> buildBipolar(const QStringList& channelNames);

    // Build common-average reference: each channel minus the mean of all channels
    static QVector<DerivationRule> buildCommonAverage(const QStringList& channelNames);

    // Apply derivation rules to raw data
    // matData: n_channels × n_times
    // channelNames: names corresponding to matData rows
    // Returns derived data (n_derived × n_times) and derived channel names
    static QPair<Eigen::MatrixXd, QStringList> apply(
        const Eigen::MatrixXd& matData,
        const QStringList& channelNames,
        const QVector<DerivationRule>& rules);

    // Parse derivation rules from a definition file (text format like MNE-C mne_make_derivations)
    static QVector<DerivationRule> readDefinitionFile(const QString& path);
    static bool writeDefinitionFile(const QString& path, const QVector<DerivationRule>& rules);
};

}
```

### Acceptance criteria
- **Bipolar grouping**: `buildBipolar({"LH1","LH2","LH3","RA1","RA2"})`
  returns exactly 3 rules: `LH1-LH2` (weights: LH1=+1, LH2=-1),
  `LH2-LH3` (weights: LH2=+1, LH3=-1), `RA1-RA2` (weights: RA1=+1, RA2=-1).
- **Numerical correctness**: Create a 5×1000 data matrix where channel i has
  constant value i. Bipolar derivation LH1-LH2 should be constant=-1 for all
  samples. Verify all derived channels.
- **Data-driven test**: Load real sEEG data from `mne-cpp-test-data/` (if
  available) or synthesise 64-channel sEEG-like data with 8 shafts × 8
  contacts. Apply bipolar derivation and verify output has 56 channels
  (7 per shaft × 8 shafts).
- **Common average reference**: After CAR, the mean across all output
  channels at every time point is < 1e-12 (machine epsilon).
- **Custom weights**: A rule with weights {"A": 0.5, "B": 0.3, "C": 0.2}
  produces `0.5*A + 0.3*B + 0.2*C` exactly.
- **Definition file round-trip**: Write rules → read back → compare: same
  number of rules, same names, same weights (to 1e-15 precision).
- **Edge cases**: single channel (no pairs → empty result), channels with
  no numeric suffix (each is its own shaft), duplicate channel names (error).
- **Coverage**: ≥75% line coverage for `channel_derivation.cpp`.

---

## TASK 13 — sEEG Contact Localization (BIDS Import)

### Goal
Extend the existing BIDS electrode import (`BidsElectrode::readTsv()` in
`src/libraries/bids/bids_electrode.h`) with coordinate system handling and
optional atlas region lookup. This enables automatic labeling of electrode contacts
with anatomical region names from a FreeSurfer parcellation.

### Files to create/modify

| File | Action | Purpose |
|------|--------|---------|
| `bids/bids_coordinate_system.h/.cpp` | CREATE | Parse `*_coordsystem.json`, return `FiffCoordTrans` to MRI |
| `bids/bids_electrode.h/.cpp` | MODIFY | Add `toFiffDigPoints()` that converts electrodes + coord transform to `FiffDigPointSet` |
| `fs/fs_atlas_lookup.h/.cpp` | CREATE | `FsAtlasLookup` — given a FreeSurfer volume parcellation (aparc+aseg.mgz), look up the atlas label for an RAS coordinate |

### `FsAtlasLookup` design
```
namespace FSLIB {

class FSSHARED_EXPORT FsAtlasLookup
{
public:
    // Load a FreeSurfer volume parcellation (aparc+aseg.mgz)
    bool load(const QString& parcellationPath);

    // Look up the atlas label at a given RAS coordinate
    QString labelAtRas(const Eigen::Vector3f& ras) const;

    // Look up labels for a list of electrode positions
    QStringList labelsForElectrodes(const QList<BIDSLIB::BidsElectrode>& electrodes,
                                     const FIFFLIB::FiffCoordTrans& toRas) const;

private:
    MRILIB::MriVolData m_parcellation;
    QMap<int, QString> m_lookupTable;   // voxel value → region name
};

}
```

### Acceptance criteria
- **Coordinate system parse**: `BidsCoordinateSystem::read()` on a valid
  BIDS `*_coordsystem.json` with `iEEGCoordinateSystem: "Other"` and a 4×4
  `iEEGCoordinateProcessingDescription` matrix returns a `FiffCoordTrans`
  with correct from/to frames and the exact 4×4 transform values.
- **Electrode to FiffDigPointSet**: Convert 12 electrodes from TSV + coord
  transform. Verify resulting `FiffDigPointSet` has 12 points, each with
  the correct 3D position (multiply TSV xyz by coord transform, compare
  to within 0.001 mm).
- **Data-driven atlas lookup**: Load FreeSurfer `aparc+aseg.mgz` from
  `mne-cpp-test-data/` (or FreeSurfer sample subject). Verify:
  - RAS coordinate at (0, -25, -15) maps to a hippocampal label
    ("Left-Hippocampus" or similar).
  - RAS coordinate at (0, 0, 50) maps to a cortical label (e.g.
    "ctx-lh-superiorfrontal" or "Unknown" if in white matter).
  - RAS coordinate far outside the brain (100, 100, 100) maps to "Unknown".
- **Batch lookup**: `labelsForElectrodes()` on 12 electrodes returns 12
  labels, none empty.
- **Edge cases**: electrode with `x="n/a"` (skip gracefully), coordsystem
  JSON with missing required field (return error, don't crash).
- **Coverage**: ≥75% line coverage for `bids_coordinate_system.cpp` and
  `fs_atlas_lookup.cpp`.

---

## TASK 14 — Ledoit-Wolf Shrinkage Covariance (Bonus)

### Goal
Add the Ledoit-Wolf optimal shrinkage estimator to
`src/libraries/fiff/fiff_cov.cpp` or a new helper, enhancing covariance
estimation for rank-deficient M/EEG data (e.g. after SSS/tSSS).

### Why
The current empirical covariance + Tikhonov regularisation requires manual tuning.
Ledoit-Wolf automatically computes the optimal shrinkage coefficient toward a
structured target (scaled identity), producing better-conditioned covariance
matrices for inverse solutions without user parameters.

### Where to add
Add a static method to the existing `FiffCov` class or create a small helper:

```
// In fiff_cov.h or a new file cov_estimators.h
class FIFFSHARED_EXPORT CovEstimators
{
public:
    // Ledoit-Wolf shrinkage estimator
    // matData: n_channels × n_samples (zero-mean)
    // Returns: shrunk covariance (n_channels × n_channels) and shrinkage coefficient alpha
    static QPair<Eigen::MatrixXd, double> ledoitWolf(const Eigen::MatrixXd& matData);
};
```

### Algorithm
Analytical formula from Ledoit & Wolf (2004):
1. Compute sample covariance `S = X X^T / (n-1)`.
2. Target `F = trace(S)/p * I` (scaled identity).
3. Compute optimal shrinkage `α` using the Ledoit-Wolf formula
   (involves computing the sum of squared off-diagonal elements and a
   bias correction term proportional to 1/n).
4. Return `α*F + (1-α)*S`.

### Acceptance criteria
- **Data-driven rank test**: Load real noise covariance from
  `mne-cpp-test-data/` (has rank < n_channels due to SSS). Apply Ledoit-Wolf
  to the raw data. Verify resulting covariance is full-rank with condition
  number < 1e6 (while the sample covariance has condition number > 1e12).
- **Synthetic test**: Generate 64-channel Gaussian data with true rank 50
  (first 50 components have variance 1.0, last 14 have variance 0). Apply
  Ledoit-Wolf. Verify:
  - Shrinkage coefficient α ∈ [0, 1].
  - Result is positive definite (all eigenvalues > 0).
  - Condition number < 1e6.
- **Reference validation**: Compute Ledoit-Wolf on a fixed 10×10 dataset and
  compare α and the resulting covariance to scikit-learn
  `sklearn.covariance.LedoitWolf()` output (values match to 6 decimal places;
  embed reference values as test constants).
- **Identity limit**: For data where all channels are i.i.d. standard normal
  (n_samples >> n_channels), α should be close to 0 (sample covariance is
  already well-conditioned).
- **Edge cases**: n_samples < n_channels (severely rank-deficient, α should
  be close to 1), single sample (should fail gracefully), constant channel.
- **Coverage**: ≥75% line coverage for the Ledoit-Wolf implementation.

---

## Build Integration Summary

After all tasks, `src/libraries/CMakeLists.txt` should read (new lines marked):

```cmake
add_subdirectory(math)
add_subdirectory(utils)
add_subdirectory(lsl)

add_subdirectory(fiff)
add_subdirectory(bids)
add_subdirectory(fs)
add_subdirectory(mri)

add_subdirectory(mne)
add_subdirectory(fwd)
add_subdirectory(inv)
add_subdirectory(com)
add_subdirectory(conn)
add_subdirectory(dsp)

add_subdirectory(mna)       # NEW — depends on utils, Qt6::Core
add_subdirectory(ml)        # NEW — depends on utils, math, Qt6::Core, eigen; optionally ONNX Runtime
add_subdirectory(stats)     # NEW — depends on utils, math, fiff, Qt6::Core, eigen

add_subdirectory(disp)

add_subdirectory(disp3D)
```

New libraries (`mna`, `ml`, `stats`) are placed after `dsp` (they may depend on it)
and before `disp` (display libraries may depend on them for data types).

---

## TASK 15 — Release Wrap-Up: Code Quality, Documentation & Website

### Goal
Before tagging v2.2.0, perform a comprehensive quality pass across all new and
modified code. This is a gate-keeping task — nothing ships until every item here
is checked off.

### 1. Static analysis & code smells

- **Coverity / CodeQL**: Run the existing CI workflows (`codeql.yml`,
  Coverity Scan). Zero new defects in new code. Any pre-existing issues in
  files touched by v2.2.0 work should also be fixed.
- **Compiler warnings**: Build with `-Wall -Wextra -Wpedantic` (GCC/Clang) or
  `/W4` (MSVC). Zero warnings in new/modified files.
- **clang-tidy**: Run at least the following check groups on all new files:
  `bugprone-*`, `performance-*`, `readability-*`, `modernize-*`.
  Fix all findings or add a `NOLINT` with justification.
- **Code smells to hunt for**:
  - Raw `new`/`delete` → prefer `QScopedPointer`, `std::unique_ptr`, RAII.
  - `#include` bloat → forward-declare where possible; verify no circular deps.
  - Magic numbers → extract named constants.
  - Copy-paste duplication → factor into shared helpers.
  - Functions > 80 lines → consider splitting.
  - Deeply nested logic (> 3 levels) → refactor early returns / helper methods.
  - Non-const reference parameters that should be const.
  - Missing `override` on virtual method overrides.
  - Unused `#include`s (use `iwyu` or manual review).

### 2. Doxygen documentation

- **Every new public class, struct, enum, and free function** must have a
  Doxygen comment block (`/** ... */`) with:
  - `@brief` one-liner
  - `@param` for every parameter
  - `@return` for non-void functions
  - `@since 2.2.0`
- **Every new header file** must have the standard file-level Doxygen block
  (`@file`, `@author`, `@since`, `@date`).
- Run `doxygen doc/doxygen/Doxyfile` (or equivalent) and verify:
  - Zero Doxygen warnings for new files.
  - Generated API pages render correctly for all new classes.

### 3. README.md update

Update the root `README.md` to reflect v2.2.0 additions:

- **Version badge**: Change `2.1.0` → `2.2.0`.
- **Libraries table**: Add rows for `mna`, `ml`, `stats`:
  | Library | Description |
  |---------|-------------|
  | **Mna** | Analysis container format — `.mna` (JSON) / `.mnx` (BSON) project manifests with provenance |
  | **Ml** | Machine learning — ONNX Runtime inference, feature pipelines, scalers |
  | **Stats** | Statistical testing — cluster permutation, t-test, F-test, FDR correction |
- **Inv library description**: Update to mention CMNE, MxNE, Gamma-MAP,
  Contextual MNE alongside the existing methods.
- **Dsp library description**: Update to mention multitaper PSD/TFR, CSD,
  Extended Infomax ICA, bipolar re-referencing.
- **Applications table**: If MNE Inspect gains electrode viz or MRI slicing,
  update its description accordingly.

### 4. CHANGELOG.md update

Add a `## v2.2.0 — "Intracranial + Intelligence"` section with:
- **New libraries**: `mna`, `ml`, `stats` (one bullet each with summary).
- **New inverse methods**: Contextual MNE (CMNE), MxNE, Gamma-MAP.
- **New DSP features**: Multitaper PSD/TFR, cross-spectral density, Extended
  Infomax ICA, bipolar re-referencing, Ledoit-Wolf covariance.
- **New sEEG features**: stereotactic depth electrode visualization,
  contact localization, bipolar re-referencing.
- **New visualization**: MRI volume slicing, stereotactic electrode rendering.
- **FiffAnnotations**: New library-level annotation class.
- **Infrastructure**: `.mna`/`.mnx` container format, ONNX Runtime integration.
- **Bug fixes / improvements** accumulated during development.

### 5. Website update (Docusaurus)

The website lives at `doc/website/` and deploys to `mne-cpp.github.io`.

- **`docs/overview.mdx`**: Update feature list to include new libraries,
  sEEG capabilities, CMNE, and `.mna/.mnx` format.
- **`docs/cite.mdx`**: Add the CMNE publication (Dinh et al. 2021) to the
  citations section.
- **`docs/publications.mdx`**: Add CMNE paper to the publications list.
- **`docs/manual/`**: Add or update pages for:
  - New libraries overview (mna, ml, stats)
  - sEEG workflow (electrode viz, localization, bipolar re-ref)
  - CMNE usage guide (how to train in Python, export ONNX, run in mne-cpp)
  - `.mna`/`.mnx` format specification
- **`docs/development/api`**: Verify auto-generated API docs include all new
  classes (this happens via Doxygen → website pipeline).
- **Download page**: Verify that v2.2.0 binaries appear correctly once tagged
  (this is automatic from `staging.yml` / release workflow, but sanity-check
  after tagging).

### 6. CITATION.cff update

- Set `version: 2.2.0`
- Update `date-released` to the release date.
- Verify `doi` still points to the concept DOI `10.5281/zenodo.593102`.

### 7. Final CI verification

- All CI matrix jobs green (Linux static/dynamic, macOS, Windows, WASM,
  CodeQL, Coverity, code coverage).
- Code coverage ≥ 75% for each new library (`mna`, `ml`, `stats`) and ≥ 75%
  for new files in existing libraries (`inv`, `dsp`, `mri`, `disp3D`).
- No test uses `QSKIP()` except for the "test-data directory absent" guard.
- All tests pass — none skipped, none failing.

### 8. License and copyright headers

- Every new `.h` and `.cpp` file has the BSD-3-Clause header with correct
  copyright year (2026) and author attribution.
- Third-party code (if any) has its license noted in the file header and
  in `LICENSE` or a `THIRD_PARTY_LICENSES` file.

### Acceptance criteria
- All 8 sub-tasks above are completed.
- A reviewer (human or automated) has verified each checklist item.
- The `staging` branch is ready for the merge-to-main ceremony.

---

## Priority Order for Implementation

| Order | Task | Rationale |
|-------|------|-----------|
| 1 | TASK 4 — FiffAnnotations | Foundation: unblocks annotation I/O everywhere |
| 2 | TASK 1 — `mna` library | Foundation: project container for all workflows |
| 3 | TASK 5 — Multitaper PSD/TFR | Foundation: required by CSD (Task 6) |
| 4 | TASK 6 — CSD | Foundation: required by improved DICS + connectivity |
| 5 | TASK 12 — Bipolar re-referencing | Foundation: required before sEEG analysis |
| 6 | TASK 3 — `stats` library | Core analysis feature |
| 7 | TASK 8 — MxNE + Gamma-MAP | Sparse inverse methods |
| 8 | TASK 9 — Contextual MNE (CMNE) | Published method (Dinh et al. 2021) — unique to mne-cpp, LSTM-based |
| 9 | TASK 7 — Extended Infomax ICA | Preprocessing improvement |
| 10 | TASK 2 — `ml` library | ML infrastructure |
| 11 | TASK 13 — sEEG contact localization | Requires BIDS + FS libraries |
| 12 | TASK 11 — MRI volume slicing | Visualization |
| 13 | TASK 10 — Stereotactic depth electrode (sEEG) viz | Visualization (most complex) |
| 14 | TASK 14 — Ledoit-Wolf covariance | Bonus if time permits |
| 15 | TASK 15 — Release wrap-up | Gate: quality, docs, website, README, CHANGELOG |

---

## Version Tagging

When all tasks are merged to staging and CI is green:
1. Update `CHANGELOG.md` with all new features.
2. Update `CITATION.cff` version to `2.2.0`.
3. Merge staging → main.
4. Tag `v2.2.0` on main.
5. GitHub Release with binaries triggers automatically.
